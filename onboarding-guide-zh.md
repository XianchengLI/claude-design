# Claude Code 从 0 到 1：我的完整构建与工作模式分享

> 作者：Xiancheng | 2026-08 | 基于约 6 个月的日常重度使用（学术研究 + 数据分析场景）
> 本文自包含，不依赖我机器上的任何其他文件，可直接阅读入门。

---

## 0. 先建立心智模型

Claude Code 不是"聊天窗口"，而是一个**在你电脑上干活的 agent**：它能读写文件、跑命令、搜代码、调外部工具，在一个循环里自主完成多步任务。你要做的不是"提问"，而是**给它一个可交付的任务 + 足够的上下文**。

三个最重要的概念：

1. **Context（上下文）是稀缺资源。** 每次对话有一个 context window，塞进去的所有东西（你的配置、文件内容、工具输出）都占空间、都花钱。整套配置体系的核心问题就是：**什么信息应该常驻，什么信息应该按需加载。**
2. **配置是分层的。** 全局（对所有项目生效）→ 项目级（只对当前 repo 生效）→ 会话级。同一个机制（比如 CLAUDE.md、skills）在每层都可以放。
3. **"教 Claude 做事"有多种机制，成本和能力各不相同。** 选错机制是新手最常见的浪费（比如把一次性需求写成 MCP server）。

### 各机制对比（我认为最值得先看懂的一张表）

| 机制 | 本质 | 增加新能力？ | Context 成本 | 适合放什么 |
|------|------|-------------|-------------|-----------|
| **CLAUDE.md** | 常驻指令 | 否 | **全文每次会话都加载** | 少量、高频、必须遵守的规则 |
| **Skill** | 按需加载的工作流说明书 | 否（纯 prompt） | 平时只占 1 行描述 | 复杂但低频的流程 |
| **Command**（斜杠命令） | 手动触发的 prompt 模板 | 否 | 触发时才加载 | 你主动发起的固定流程 |
| **Prompt 文件库** | 让 Claude `Read` 的模板 | 否 | 被读取时才占 | 更轻量的可复用 prompt |
| **Hook**（生命周期脚本） | 会话开始/结束等时机自动跑的外部脚本 | 否 | 零 | 自动化（备份、注入状态） |
| **MCP Server** | 独立进程提供的结构化新工具 | **是** | **所有工具定义常驻注入** | 高频、有状态、结构化的外部服务 |

一条经验法则：**能用 Bash/WebFetch 一两步搞定的，不要上 MCP；只做一次的，不要写 skill；每次都要遵守的，才进 CLAUDE.md。**

---

## 1. 我的目录总览

```
~/.claude/                        ← 全局层（对所有项目生效）
├── CLAUDE.md                     ← 全局个人规则（~50 行，刻意精简）
├── commands/                     ← 全局斜杠命令（5 个）
│   ├── academic.md               ← 学术写作流程路由器
│   ├── debate.md                 ← 多轮对抗性头脑风暴
│   ├── vault.md                  ← 查询个人知识库
│   ├── log-to-vault.md           ← 把当前会话成果记录进知识库
│   └── review-writing.md         ← 学术文本修改的 subagent 审稿
├── skills/                       ← 全局 skills（latex-to-word、文献检索等）
├── prompts/                      ← 学术写作 prompt 库（7 阶段 pipeline + 8 个模板）
├── hooks/                        ← session-start / session-end / pre-compact 脚本
├── agents/                       ← 自定义 subagent 定义（python-pro、debugger 等）
└── settings.json                 ← 权限白名单、hooks 注册、statusline、插件

每个项目/                          ← 项目层
├── CLAUDE.md                     ← 项目专属指令
└── .claude/
    ├── rules/                    ← 拆分出去的细则（CLAUDE.md 里只留索引）
    ├── skills/                   ← 项目专属 skills
    └── settings.json             ← 项目级权限

~/Documents/The Vault/            ← 特殊项目：Obsidian 知识库 = 外部记忆系统
~/Documents/claude-design/        ← 跨项目的 Claude Code 设计笔记（本文所在地）
```

---

## 2. CLAUDE.md：常驻规则怎么写

CLAUDE.md 是每次会话都全文加载的指令文件。**新手最大误区是把它写成百科全书**——我曾把一个项目的 CLAUDE.md 写到 684 行，后来砍到 110 行，把历史记录移到单独文档，效果反而更好（Claude 更听话、更省 context）。

### 我的全局 CLAUDE.md 里放了什么（每条都是踩坑后加的）

- **语言规则**：对话用中文，代码/文档/commit 用英文。
- **Stress-Test 规则（我最推荐的一条）**：当我用不确定的语气（"可能"、"我觉得"、"或者？"）谈论**决策层面**的事（方法选择、论证框架、研究方向）时，Claude 不许直接照做，必须先钢人化（steelman）一个真实的替代方案、诚实比较利弊，再报告"你的框架经得起检验，因为 X"或"有个更好的框架你漏了：Y"。同时明确**禁止捏造分歧**——真审视后同意也是合法结果。这条规则把 Claude 从"顺从的执行者"变成了"会顶嘴的合作者"，对研究工作价值极大。
- **API 实验铁律**：任何花钱的 API 调用，必须先把逐条原始结果存成 CSV 再打印摘要。重跑就是烧钱。
- **写作修改的 Diff Preview 工作流**：对已有文稿的精修，不直接改源文件，而是写一个只含 `## Before` / `## After` 两块的 markdown 暂存文件让我预览，我说"apply"才动真正的源文件。理由放聊天里，不放文件里。
- **引用核查铁律**：碰引用必须逐条做**内容级**核查（全文优先，摘要级要显式声明并记 TODO），DOI 能解析不算核查。这条规则来自血泪：一次审计发现 13 处引用问题，其中最主要的缺陷模式是"attribution creep"——把自己的解读写成了被引文献的发现。
- **不要中途停下**：工具返回结果后默认继续干，不要等我说"继续"。

### 写 CLAUDE.md 的原则

1. **每条规则都要有"为什么"**——没有场景支撑的规则 Claude 执行得很差。
2. **标 CRITICAL 的规则要克制**，全都 CRITICAL 等于没有 CRITICAL。
3. **细则拆出去**：项目 CLAUDE.md 里放索引，细节放 `.claude/rules/*.md`，Claude 需要时自己去读。
4. **定期瘦身**：规则是负债不是资产，过时的删掉。

---

## 3. Commands（斜杠命令）：我用得最多的机制

Command 就是一个 markdown 文件，放在 `~/.claude/commands/`（全局）或 `项目/.claude/commands/`，文件名即命令名。输入 `/名字` 时文件内容作为指令注入。**这是性价比最高的定制机制**：零常驻成本、写起来就是写一篇操作说明。

我的 5 个全局命令，按设计思路分享：

### `/vault` + `/log-to-vault` —— 一对读写命令，构成"外部记忆系统"

我有一个 Obsidian 知识库（"The Vault"），记录所有项目状态、人物、决策、日志。这对命令让**任何项目里的 Claude** 都能：

- `/vault <问题>` —— 查库回答（"Pietro 是谁"、"SB 项目的网络分析结果"）。命令文件里写了一张"主题 → 去哪个目录找"的路由表。
- `/log-to-vault [提示]` —— 分析当前会话，**自动提炼**值得长期保存的内容（决策、里程碑、发现、状态变化）写进库里。关键设计：不是复读我给的提示，而是命令文件里明确列出"捕捉什么（决策/里程碑/发现）、跳过什么（调试过程、中间尝试）"。

这个模式我强烈推荐：**跨项目共享记忆不需要任何花哨技术，一个文件夹 + 两个命令就够了。**

### `/academic` —— 路由器模式（router command）

学术写作有一条 7 阶段 pipeline（结构对齐 → 内容起草 → 逻辑检查 → 语言润色 → 字数控制 → 自审 → 投稿前检查），每阶段有对应 prompt 模板。但我不想每次手动想"现在该跑哪步"。

`/academic` 的设计：它**先侦察**（读 pipeline 文档、扫当前目录的稿件文件、读断点文件 `claims.md` 的 Pipeline State 一节），然后**提出 2-3 个具体执行方案让我选**，我确认前不许动手。

> 可复用的模式：**当你有一套多阶段流程时，别为每阶段建一个命令，建一个会"评估现状 → 提方案 → 等确认"的路由器。** 断点文件（记录当前做到哪一步）让流程能跨会话续跑。

### `/debate` —— 对抗性思考命令

`/debate <话题>` 跑多轮双轨对抗推理，**刻意保留分歧**而不是强行收敛：输出"双方同意什么、仍分歧什么、哪些只能我自己决定"。另有 `/debate converge` 变体用于必须收敛出唯一答案的场景。用于想认真掰扯的决策（选题方向、方法取舍）。

### `/review-writing` —— Subagent 审稿模式

修改学术文本后，`/review-writing` 会启动一个**独立的 subagent** 按检查清单审计刚才的修改稿，然后基于审稿意见再修一轮，把成熟版给我。核心洞察：**让同一个上下文里的 Claude 自己审自己效果差**，起一个干净上下文的 subagent 当审稿人，批评质量明显更高。

---

## 4. Skills：按需加载的"说明书"

Skill = 一个文件夹里的 `SKILL.md`（可带辅助脚本/资源），平时只有一行描述占 context，Claude 判断任务匹配时才全文加载。适合**复杂但不是每次都用**的流程。

我的全局 skills 举例：
- `latex-to-word` —— LaTeX 稿件转投稿用 Word（pandoc + TikZ 图单独渲染 + LibreOffice 视觉 QA），流程有十几步，正是 skill 的典型场景。
- `nature-*` 系列 —— 文献检索、引用添加、论文转 PPT、审稿回复信等（改自开源的 nature-skills 仓库，装完做过一轮瘦身）。
- 项目级 skills —— 比如研究 repo 里装 `literature-review`、`scientific-writing`、`humanizer`（去 AI 写作痕迹）、`statistical-analysis`。**领域 skills 装项目层，别全塞全局。**

### SKILL.md 怎么写（从一个 3.5k star 的开源 skill 学来的模板）

复杂 skill 的 8 层结构：

```
1. frontmatter      — name + description（触发匹配靠它）
2. When to Use      — 显式列触发条件和例句（别只靠 description）
3. Critical Rules   — 正确/错误写法对照（Claude 爱抄近路，错误示例是护栏）
4. Core Workflow    — 带精确命令的分步流程
5. Decision Flow    — 完整决策树（防跳步、防死循环）
6. Behavior Rules   — 拿到结果之后该做什么（默认行为是"拿到就回"，要覆盖它）
7. Troubleshooting  — 常见错误 → 修法对照表（防 Claude 陷入重试循环）
8. Limitations      — 明说做不到什么（防 Claude 承诺不存在的功能）
```

**但简单 skill 不要过度工程**：几十行的小流程，"任务 + 步骤 + 风格"三段就够。判断标准：涉及外部工具调用、有条件分支、容易出错、需要解读结果——才值得上完整模板。

---

## 5. Prompt 文件库：比 skill 更轻的复用

`~/.claude/prompts/` 下放纯 prompt 模板（英文润色、审稿人视角自审、逻辑检查、压缩字数、扩写……），配一个 README 索引表 + 一个 WORKFLOW.md 把它们串成 pipeline。

用法就是自然语言："读 `~/.claude/prompts/academic-polish.md`，然后润色这段"。零配置、零常驻成本。**很多人第一反应写 skill 的东西，其实一个 prompt 文件就够了**——skill 的价值在自动触发和捆绑脚本，如果你总是手动发起，prompt 文件 + 命令引用更简单。

---

## 6. Hooks：生命周期自动化

Hook 是在特定时机自动执行的外部脚本（在 `settings.json` 注册），Claude 不参与执行，零 context 成本。我的三个：

- **SessionStart** —— 会话开始时注入项目名、会话文件名、记忆条目数等状态信息。
- **SessionEnd** —— 会话结束时收尾 + **自动备份对话记录**（transcript 是宝贵资产，值得备份）。
- **PreCompact** —— context 压缩前触发。压缩会丢细节，这个时机可以抢救关键状态。配套地，我在全局 CLAUDE.md 写了"压缩后必须继续用中文回复"——**压缩会冲掉行为惯性，重要行为规则要写成能在压缩后存活的形式。**

---

## 7. MCP：真正增加新工具，但要克制

MCP server 是独立进程，给 Claude 提供原生没有的结构化工具。**注意成本：所有工具定义常驻注入 context**，装 5 个大 server 你的每次对话都背着几十个工具定义。

我只装了 3 个（user 级全局）：

| Server | 提供什么 | 为什么值得 |
|--------|---------|-----------|
| `memory` | 知识图谱式跨会话记忆 | 有状态，原生做不到 |
| `academic-search` | PubMed/CrossRef/arXiv 结构化检索 | 高频 + 结构化返回，WebFetch 解析 HTML 又慢又脆 |
| `zotero` | 直接读写本地 Zotero 库（含 PDF 全文） | 有状态本地服务，引用核查工作流的核心 |

判断标准（我的笔记原文）：**"If WebFetch + Bash can do it, you don't need MCP."** 高频、有状态、结构化三占其二再考虑。

安装用 `claude mcp add <name> --scope user -- <启动命令>`。踩坑：本地 server 要装 user 级放 `~/.claude.json` 顶层的 `mcpServers`，别放项目的 `.mcp.json`（否则换个项目就没了）。

---

## 8. settings.json：权限与体验

几个值得早点配的：

- **权限白名单**（`permissions.allow`）：Claude 每类操作默认要人批。把你信任的高频操作加进白名单（`git commit`、`python`、常用域名的 WebFetch……），打断次数会大幅下降。有现成 skill 可以扫描历史记录自动生成白名单建议。
- **additionalDirectories**：默认只能访问启动目录。跨 repo 工作（比如分析 repo + Overleaf repo）要把相关目录加进来。
- **statusline / 插件**：我用 claude-hud 插件做状态栏（显示 context 用量等）。看得见 context 消耗，才会有优化它的意识。

---

## 9. 核心工作模式（比配置更重要的部分）

配置只是骨架，真正的价值在工作模式。挑最有普适性的几个：

### 9.1 外部记忆系统（The Vault）

一个 Obsidian 库当"第二大脑"，但关键是**协作协议**：

- **我说，Claude 写**：我用中文口头汇报（"今天把 XX 的 method 改完了，合作者回了邮件说……"），Claude 负责分类、路由、用英文写进正确的文件（日志 → `Daily/`，项目进展 → 项目笔记，新人物 → `People/`，可复用流程 → `Tools/`）。路由规则写成 `.claude/rules/routing-rules.md`。
- **角色边界**：在这个库里 Claude 只当记录员和管理员，**不许做项目本身的工作**（不改稿、不写代码）。项目工作在各项目 repo 里做，那里有各自的 Claude 上下文。这条边界让每个上下文都保持干净。
- 库的 CLAUDE.md 里维护一份"活跃项目状态"总表，Claude 更新。于是每次在库里开新会话，Claude 天然知道我所有项目的最新状态。

### 9.2 两 repo 学术工作流

每篇论文 = 分析 repo（代码、数据、notebook）+ Overleaf repo（git clone 的稿件）。两边各有自己的 CLAUDE.md 和上下文；用一个 SYNC.md 记录图表从分析 repo 到稿件 repo 的同步关系。**不要让一个上下文同时背着代码库和稿件库。**

### 9.3 让 Claude 可被审计

- 所有花钱实验：逐条结果先落盘再汇总（前文铁律）。
- 所有引用操作：维护 `reference_ledger.md`，每条引用标核查级别（unverified / abstract / full-text），投稿时 unverified 必须清零。
- 所有稿件修改：Before/After 暂存文件 → 人工预览 → 显式 "apply" 才落盘。

共同思想：**Claude 的产出必须经过一个人能低成本审查的关口，而不是直接落到最终产物上。**

### 9.4 Subagent 的用法

两个高价值场景：
1. **干净上下文审稿**（`/review-writing` 模式）：审查者不该被创作时的上下文污染。
2. **大范围搜索/侦察**：让 subagent 去翻几十个文件，只把结论带回来，主上下文不被文件内容淹没。

### 9.5 复盘进配置的闭环

每次踩坑或发现好模式，问一句"这该进哪层配置？"——高频规则进 CLAUDE.md，流程进 command/skill，事实进 memory，坑写进设计笔记（我为此单开了 `claude-design/` 目录存跨项目的模式笔记，本文就是其中一篇）。**配置体系是长出来的，不是一天设计出来的。**

---

## 10. 给从 0 开始的你：建议路径

**第 1 周 —— 裸用。** 什么都不配，直接在真实项目里用。先建立"它能做什么/哪里会烦"的手感，否则你不知道该配什么。

**第 2 周 —— 第一个 CLAUDE.md。** 把这周你重复说过 3 遍以上的话写进去（语言偏好、代码风格、"先计划再写码"）。保持 20 行以内。跑 `/init` 让它给项目生成初版也行。

**第 3-4 周 —— 第一个 command。** 找一个你每次都手动发起的固定流程（日报、代码审查、某种固定格式的输出），写成 `~/.claude/commands/xxx.md`。这是投入产出比最高的一步。

**第 2 个月 —— 权限白名单 + 按需 skill。** 被权限弹窗烦到了就配白名单；遇到"步骤多到 Claude 老是漏步"的流程再写 skill（用第 4 节的模板）。

**之后 —— 谨慎上 MCP 和 hooks。** 确认是高频 + 有状态 + 结构化的需求再装 MCP；hooks 留给真正的自动化需求（备份、状态注入）。

### 最后三条心法

1. **Context 是最贵的资源**，所有配置决策先问"这占多少常驻 context"。
2. **规则要带场景和理由**，光有命令式规则 Claude 遵守率很低。
3. **把 Claude 当能力很强但需要管理的合作者**：给边界（role boundary）、给审查关口（diff preview）、给顶嘴的义务（stress-test 规则），而不是当一个更快的打字员。

祝玩得开心。有问题随时来问我。
